{{- /* Primary service - only created if internalPort is set */}}
{{- if or .Values.service.internalPort (and (eq .Values.debug.enabled true) .Values.debug.port) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "appname" . }}
{{- include "app.labels" . | indent 2 }}
    app: {{ include "appname" . }}
    component: primary
spec:
  selector:
    app: {{ include "appname" . }}
    component: primary
  ports:
{{- if .Values.service.internalPort }}
  - name: {{ include "appname" . }}-port
    protocol: TCP
    port: {{ .Values.service.internalPort }}
    targetPort: {{ .Values.service.internalPort }}
{{- end }}
{{- if eq .Values.debug.enabled true }}
  - name: {{ include "appname" . }}-debug-port
    protocol: TCP
    port: {{ .Values.debug.port }}
    targetPort: {{ .Values.debug.port }}
{{- end }}
{{- end }}

{{- /* Worker service - only created if worker is enabled AND has internalPort > 0 */}}
{{- if and .Values.worker.enabled .Values.worker.service.internalPort (gt (.Values.worker.service.internalPort | int) 0) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "appname" . }}-worker
{{- include "app.labels" . | indent 2 }}
    app: {{ include "appname" . }}-worker
    component: worker
spec:
  selector:
    app: {{ include "appname" . }}-worker
    component: worker
  ports:
  - name: {{ include "appname" . }}-worker-port
    protocol: TCP
    port: {{ .Values.worker.service.internalPort }}
    targetPort: {{ .Values.worker.service.internalPort }}
{{- end }}

---

{{- if .Values.api_gateway.expose }}
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: {{ include "appname" . }}-healthcheck
spec:
  default:
    checkIntervalSec: 10
    timeoutSec: 2
    healthyThreshold: 2
    unhealthyThreshold: 1
    logConfig:
      enabled: true
    config:
      type: HTTP
      httpHealthCheck:
        portSpecification: USE_FIXED_PORT
        port: {{ .Values.service.internalPort }}
        host: "gcp-lb-health-check"
        requestPath: {{ .Values.api_gateway.health_endpoint }}
  targetRef:
    group: ""
    kind: Service
    name: {{ include "appname" . }}
{{- end }}
